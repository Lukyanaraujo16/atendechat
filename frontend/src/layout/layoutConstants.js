/**
 * Altura unificada: AppBar / toolbar, área da logo no drawer, spacer do conteúdo.
 * Manter sincronizado em layout principal e layouts derivados (ex.: SaaS).
 */
export const APP_HEADER_HEIGHT = 64;

/**
 * ModuleTabsLayout (`moduleWrap`): `theme.spacing(2)` à esquerda + direita em `md+`.
 * Padding horizontal total subtraído da largura útil do módulo.
 */
export const MODULE_TABS_HORIZONTAL_PADDING_PX = 32;
