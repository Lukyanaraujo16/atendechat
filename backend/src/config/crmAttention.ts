/** Código estável guardado em `attentionReason`; UI traduz via i18n. */
export const CRM_ATTENTION_REASON_NO_ACTIVITY_3D = "NO_ACTIVITY_3D";

/** Marcação via automação configurável (card parado). */
export const CRM_ATTENTION_REASON_AUTOMATION_STALE = "AUTOMATION_STALE";

/** Marcação genérica por regra de automação. */
export const CRM_ATTENTION_REASON_AUTOMATION_RULE = "AUTOMATION_RULE";

/** Inactividade em dias para marcar atenção (regra fixa nesta fase). */
export const CRM_STALE_ATTENTION_DAYS = 3;
