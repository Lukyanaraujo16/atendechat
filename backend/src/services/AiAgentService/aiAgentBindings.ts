/**
 * Reservado para vínculos futuros (conexões WhatsApp/Instagram, filas, etc.).
 * Quando existir associação, bloquear exclusão aqui.
 */
export async function assertNoAiAgentBindings(_agentId: number): Promise<void> {
  void _agentId;
}
